#include <iostream>
#include "Controller.h"

int main() {
    // Create an instance of the Controller class
    auto controller = Controller();
    // Call the run() function on the controller object
    controller.run();

    return EXIT_SUCCESS;
}
